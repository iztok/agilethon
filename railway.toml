[build]
builder = "nixpacks"
buildCommand = "DATABASE_URL=postgresql://build:build@localhost/build npx next build --no-turbopack"

[deploy]
startCommand = "npx prisma db push && npm run db:seed ; npm run start"
restartPolicyType = "on_failure"
