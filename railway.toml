[build]
builder = "nixpacks"
buildCommand = "DATABASE_URL=postgresql://build:build@localhost/build npm run build"

[deploy]
startCommand = "npx prisma migrate deploy && npm run start"
restartPolicyType = "on_failure"
